# Memory Derived Only From User Typed Context

Tab's MVP derives Personal Memory only from user-authored typing context observed by the native app. It does not scrape active application buffers, full documents, terminal output, server logs, screenshots, clipboard contents, or other passive content sources for memory creation.
