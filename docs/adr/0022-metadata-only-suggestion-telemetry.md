# Metadata Only Suggestion Telemetry

Tab may record suggestion interaction telemetry such as shown, accepted, dismissed, stale, latency, active application bundle ID, and suggestion length, but it does not store raw typing context or raw suggestion text as telemetry by default. This supports product quality measurement without creating a backdoor typing log.
