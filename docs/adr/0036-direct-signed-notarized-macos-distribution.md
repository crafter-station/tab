# Direct Signed Notarized macOS Distribution

Tabb distributes the macOS app directly from the web app as a signed and notarized download rather than targeting the Mac App Store for MVP. Direct distribution better fits a native autocomplete app that needs global input monitoring, accessibility permissions, fast iteration, and Electron auto-updates.
