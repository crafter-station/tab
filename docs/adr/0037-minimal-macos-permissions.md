# Minimal macOS Permissions

Tabb's MVP should require only the macOS permissions needed for typing-context observation, active application awareness, global acceptance, and suggestion insertion, expected to be Accessibility and possibly Input Monitoring. The MVP should not require Screen Recording or Full Disk Access.
