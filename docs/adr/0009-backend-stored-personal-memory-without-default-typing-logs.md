# Backend Stored Personal Memory Without Default Typing Logs

Tabb stores durable Personal Memory in the backend database to make suggestions feel personal, while raw typing logs are not stored by default. Personal Memory must be visible to and controllable by the user, and memory creation must include safeguards that avoid storing secrets or sensitive information.
