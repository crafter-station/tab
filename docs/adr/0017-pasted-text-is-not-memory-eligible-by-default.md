# Pasted Text Is Not Memory Eligible By Default

Tab may use pasted text as short-lived typing context for immediate suggestion generation after local redaction, but pasted text is not eligible for Personal Memory creation by default. MVP memory creation is based on user-authored typing context rather than pasted content that may contain third-party text, logs, configuration, documents, or secrets.
