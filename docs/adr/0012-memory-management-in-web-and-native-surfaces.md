# Memory Management In Web And Native Surfaces

Tab exposes Personal Memory controls in both the native Electron app and the TanStack Start web app, backed by the same backend API. The web app provides the full account-level memory control plane, while the native app provides quick review, deletion, pause, and trust controls where suggestions are experienced.
