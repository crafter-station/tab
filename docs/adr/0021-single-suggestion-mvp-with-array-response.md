# Single Suggestion MVP With Array Response

Tabb's suggestion API uses an array-shaped `suggestions` response for future flexibility, but the MVP returns at most one suggestion and the native overlay displays only the first item. A future interaction may let users request a different suggestion when the current one is not useful.
