# Non Streaming Suggestions For MVP

Tabb's MVP requests and displays one short suggestion at a time instead of streaming tokens into the overlay. This keeps cancellation, stale-context checks, rate limiting, logging, and overlay behavior simple while the core acceptance loop is being proven.
