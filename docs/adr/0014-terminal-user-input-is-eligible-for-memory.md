# Terminal User Input Is Eligible For Memory

Tab treats user-authored typing context in terminal applications as eligible for suggestions and Personal Memory, including prose written inside terminal-based tools such as OpenCode. Terminal command output, server logs, and passive buffer content are not eligible memory sources by default, and terminal-derived memory still passes sensitive-data guardrails before persistence.
