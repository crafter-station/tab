# Default Enabled Typing Observation With Global Opt Out

Tab's MVP observes typing context across active applications by default after the user grants macOS permissions, with a global opt-out/pause control. Per-application allow and deny controls are deferred until after MVP, while the backend still must avoid storing secrets or sensitive information and raw typing logs remain out of scope by default.
