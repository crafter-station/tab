# GitHub token used by gh CLI inside Sandcastle Docker sandboxes.
# Create a classic PAT or fine-grained token with `repo` (and `issues`) scope,
# or run `gh auth token` and paste the value here.
GITHUB_TOKEN=
